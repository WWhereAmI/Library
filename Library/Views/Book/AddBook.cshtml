@model Library.ViewModels.BookVm.BookAddVm


<div>
    @using (Html.BeginRouteForm("Default", new { controller = "Book", httproute = "true" }, FormMethod.Post, new { @class = "form-horizontal module-body", role = "form", id = "book-add-form" }))
    {
        <div class="form-group">
            <div class="col-sm-12">
                @Html.LabelFor(e => e.Title)
                @Html.TextBoxFor(e => e.Title, new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-12">
                @Html.LabelFor(e => e.Description)
                @Html.TextBoxFor(e => e.Description, new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-12">
                @Html.LabelFor(e => e.Author)
                @Html.TextBoxFor(e => e.Author, new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-12">
                <button type="submit" class="btn btn-danger">Добавить</button>
            </div>
        </div>

    }
</div>

<script>
    (function () {
        'use strict';

        $(document).ready(function () {
            $('#book-add-form').submit(function (event) {
                event.preventDefault()
                var formData = $(this).serialize();

                $.ajax({
                    url: '/Book/PostAdd',
                    method: 'POST',
                    data: formData,
                    success: function () {

                        redrawBookTable();

                        $("#editContainer").empty();

                    },
                    error: function (xhr, status, error) {

                        console.error(xhr, status, error);
                    }
                });
            });
        });

    })();
</script>




